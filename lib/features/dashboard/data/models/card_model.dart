class CardModel {
  final String cardName, cardNumber, expiredDate, cvv;

  CardModel(
      {required this.cardName,
      required this.cardNumber,
      required this.expiredDate,
      required this.cvv});
}
